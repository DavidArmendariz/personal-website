import { white } from '../../constants/colors';
import { makeStyles } from '@material-ui/core/styles';

export const linksStyles = makeStyles({
  icon: {
    color: white,
  },
  link: {
    color: white,
    fontWeight: 'bold',
  },
});
